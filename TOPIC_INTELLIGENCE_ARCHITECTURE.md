# Topic Intelligence System - Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         iOS App (Swift)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐      ┌────────────────────────────────┐      │
│  │ CustomTopic  │      │  TopicIntelligenceHelper       │      │
│  │ View         │◄────►│  - API Client                  │      │
│  │              │      │  - SwiftUI Components          │      │
│  └──────────────┘      │  - View Modifiers              │      │
│                        └────────────────────────────────┘      │
└────────────────────────────────┬────────────────────────────────┘
                                 │ REST API
                                 │ (HTTPS)
┌────────────────────────────────▼────────────────────────────────┐
│                      Backend Server (Node.js)                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │         API Routes (/api/topics/...)                      │  │
│  │  - /analyze         (Single topic)                        │  │
│  │  - /analyze-batch   (Multiple topics)                     │  │
│  │  - /learn-from-feedback                                   │  │
│  │  - /suggestions/:topic                                    │  │
│  └────────────┬─────────────────────────────────────────────┘  │
│               │                                                  │
│  ┌────────────▼─────────────────────────────────────────────┐  │
│  │      Topic Intelligence Service                           │  │
│  │  ┌──────────────────────────────────────────────────┐    │  │
│  │  │  analyzeTopicSpecificity()                        │    │  │
│  │  │  - Detects: too_broad / too_specific / just_right │    │  │
│  │  │  - Generates suggestions                          │    │  │
│  │  └──────────────────────────────────────────────────┘    │  │
│  │  ┌──────────────────────────────────────────────────┐    │  │
│  │  │  generateSearchTerms()                            │    │  │
│  │  │  - Expands specific topics                        │    │  │
│  │  │  - Refines broad topics with location             │    │  │
│  │  └──────────────────────────────────────────────────┘    │  │
│  │  ┌──────────────────────────────────────────────────┐    │  │
│  │  │  learnFromFeedback()                              │    │  │
│  │  │  - Analyzes user likes/dislikes                   │    │  │
│  │  │  - Suggests topic refinements                     │    │  │
│  │  └──────────────────────────────────────────────────┘    │  │
│  │  ┌──────────────────────────────────────────────────┐    │  │
│  │  │  fallbackAnalysis()                               │    │  │
│  │  │  - Heuristic-based (no GPT needed)                │    │  │
│  │  │  - Fast (<10ms)                                   │    │  │
│  │  └──────────────────────────────────────────────────┘    │  │
│  └────────────┬─────────────────────────────────────────────┘  │
│               │                                                  │
│  ┌────────────▼─────────────────────────────────────────────┐  │
│  │      fetchArticlesForTopic() (Enhanced)                   │  │
│  │  1. Check ArticleCache (MongoDB)                          │  │
│  │  2. Use smart search with expanded terms                  │  │
│  │  3. Fallback to NewsAPI if needed                         │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                  │
└────────────────────┬─────────────────────┬─────────────────────┘
                     │                     │
            ┌────────▼────────┐   ┌───────▼────────┐
            │   OpenAI GPT    │   │   MongoDB      │
            │   (gpt-4o-mini) │   │   (Cache)      │
            │   - Analysis    │   │   - Articles   │
            │   - Suggestions │   │   - Users      │
            └─────────────────┘   └────────────────┘
```

---

## Data Flow

### 1. User Adds Topic (Real-time Analysis)

```
User types: "Politics"
     │
     ▼
┌─────────────────────┐
│ iOS TextField       │
│ (with modifier)     │
└──────────┬──────────┘
           │ Debounce 800ms
           ▼
┌─────────────────────┐
│ POST /api/topics/   │
│      analyze        │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Topic Intelligence  │
│ Service             │
└──────────┬──────────┘
           │
           ├─► Try GPT Analysis (500-1000ms)
           │   ├─► Success → return analysis
           │   └─► Fail → use fallback
           │
           └─► Fallback Heuristics (<10ms)
               ├─► Check if broad
               ├─► Check if specific
               └─► Generate suggestions
           │
           ▼
┌─────────────────────┐
│ Return Analysis     │
│ {                   │
│   specificity,      │
│   suggestions,      │
│   message           │
│ }                   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Show Suggestions    │
│ in UI               │
│ - Warning/Info/✓    │
│ - Suggestion chips  │
│ - Add button        │
└─────────────────────┘
```

### 2. Fetching Articles (Smart Search)

```
User fetches: "RNA research"
     │
     ▼
┌─────────────────────┐
│ fetchArticlesFor    │
│ Topic()             │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Check ArticleCache  │
│ (MongoDB)           │
└──────────┬──────────┘
           │
           ├─► Category match?
           │   └─► Yes → return cached
           │
           ├─► No category match
           │   ▼
           │ ┌─────────────────────┐
           │ │ Analyze specificity │
           │ └──────────┬──────────┘
           │            │
           │  Too Specific? ──────┐
           │            │          │
           │            Yes        No
           │            │          │
           │   ┌────────▼──────┐   │
           │   │ Expand Terms  │   │
           │   │ ["rna",       │   │
           │   │  "genetics",  │   │
           │   │  "molecular"] │   │
           │   └────────┬──────┘   │
           │            │          │
           │   ┌────────▼──────────▼─┐
           │   │ Content Search       │
           │   │ (Multiple terms)     │
           │   └────────┬─────────────┘
           │            │
           │   ┌────────▼──────┐
           │   │ Score Results │
           │   │ (Primary terms│
           │   │  get higher   │
           │   │  scores)      │
           │   └────────┬──────┘
           │            │
           ▼            ▼
┌─────────────────────────┐
│ Return Articles         │
│ (15+ instead of 0-2)    │
└─────────────────────────┘
```

### 3. Learning from Feedback

```
User has 20+ articles with feedback
     │
     ▼
┌─────────────────────┐
│ Check feedback      │
│ patterns            │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ POST /api/topics/   │
│ learn-from-feedback │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Analyze feedback    │
│ - Liked articles    │
│ - Disliked articles │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ GPT Analysis        │
│ "What subtopic is   │
│  user interested?"  │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Return Refined      │
│ Topic               │
│ Confidence: 85%     │
└──────────┬──────────┘
           │
     Confidence > 70%?
           │
           Yes
           ▼
┌─────────────────────┐
│ Show Notification   │
│ "Change 'Politics'  │
│  to 'U.S. Elections'│
│  ?"                 │
└─────────────────────┘
```

---

## Component Interaction

```
┌──────────────────────────────────────────────────────────┐
│                    iOS App Layer                          │
│  ┌────────────┐  ┌────────────┐  ┌──────────────────┐   │
│  │ Custom     │  │ Personalize│  │ Topic            │   │
│  │ TopicView  │  │ View       │  │ OnboardingView   │   │
│  └──────┬─────┘  └──────┬─────┘  └────────┬─────────┘   │
│         │               │                  │              │
│         └───────────────┴──────────────────┘              │
│                         │                                 │
│         ┌───────────────▼────────────────┐                │
│         │  TopicIntelligenceHelper       │                │
│         │  - analyzeTopic()              │                │
│         │  - learnFromFeedback()         │                │
│         └───────────────┬────────────────┘                │
└─────────────────────────┼────────────────────────────────┘
                          │ HTTP/REST
┌─────────────────────────▼────────────────────────────────┐
│                   Backend Layer                           │
│  ┌───────────────────────────────────────────────────┐   │
│  │        Routes (/routes/topicIntelligence.js)      │   │
│  │  - POST /analyze                                  │   │
│  │  - POST /analyze-batch                            │   │
│  │  - POST /learn-from-feedback                      │   │
│  │  - GET  /suggestions/:topic                       │   │
│  └───────────────────┬───────────────────────────────┘   │
│                      │                                    │
│  ┌───────────────────▼───────────────────────────────┐   │
│  │   Service (/services/topicIntelligence.js)        │   │
│  │  - analyzeTopicSpecificity()                      │   │
│  │  - generateSearchTerms()                          │   │
│  │  - learnFromFeedback()                            │   │
│  │  - fallbackAnalysis()                             │   │
│  └───────────────────┬───────────────────────────────┘   │
│                      │                                    │
│         ┌────────────┴────────────┐                      │
│         │                         │                      │
│  ┌──────▼────────┐    ┌──────────▼──────────┐           │
│  │ OpenAI GPT    │    │ Heuristic Engine    │           │
│  │ (Primary)     │    │ (Fallback)          │           │
│  │ - High acc.   │    │ - Fast (<10ms)      │           │
│  │ - 500-1000ms  │    │ - No API needed     │           │
│  └───────────────┘    └─────────────────────┘           │
│                                                           │
│  ┌───────────────────────────────────────────────────┐   │
│  │   Enhanced Article Fetching (index.js)            │   │
│  │  - Uses expanded search terms                     │   │
│  │  - Smart content search                           │   │
│  │  - Relevance scoring                              │   │
│  └───────────────────┬───────────────────────────────┘   │
└────────────────────┼─────────────────────────────────────┘
                     │
┌────────────────────▼─────────────────────────────────────┐
│                   Data Layer                              │
│  ┌─────────────────┐      ┌──────────────────────────┐   │
│  │ MongoDB         │      │ OpenAI API               │   │
│  │ - ArticleCache  │      │ - GPT-4o-mini            │   │
│  │ - Users         │      │ - Topic analysis         │   │
│  │ - Feedback      │      │ - Suggestion generation  │   │
│  └─────────────────┘      └──────────────────────────┘   │
└──────────────────────────────────────────────────────────┘
```

---

## State Diagram: Topic Analysis

```
                    ┌─────────────┐
                    │  User Types │
                    │   Topic     │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │  Debounce   │
                    │  (800ms)    │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │   Analyze   │
                    │   Topic     │
                    └──────┬──────┘
                           │
                ┌──────────┴──────────┐
                │                     │
         ┌──────▼──────┐       ┌─────▼─────┐
         │   GPT       │       │ Heuristic │
         │  Available? │       │  Fallback │
         └──────┬──────┘       └─────┬─────┘
                │                    │
           ┌────┴────┐               │
           │         │               │
        Success   Timeout/Error      │
           │         │               │
           │         └───────────────┘
           │                  │
           └──────────────────┘
                      │
           ┌──────────▼──────────┐
           │  Determine          │
           │  Specificity        │
           └──────────┬──────────┘
                      │
        ┌─────────────┼─────────────┐
        │             │             │
  ┌─────▼─────┐ ┌────▼────┐ ┌─────▼─────┐
  │Too Broad  │ │Too      │ │Just Right │
  │           │ │Specific │ │           │
  └─────┬─────┘ └────┬────┘ └─────┬─────┘
        │            │            │
  ┌─────▼─────┐ ┌────▼────┐ ┌─────▼─────┐
  │Generate   │ │Expand   │ │Add        │
  │Suggestions│ │Terms    │ │Variations │
  └─────┬─────┘ └────┬────┘ └─────┬─────┘
        │            │            │
        └────────────┴────────────┘
                     │
              ┌──────▼──────┐
              │Return Result│
              │to Frontend  │
              └──────┬──────┘
                     │
              ┌──────▼──────┐
              │Show to User │
              │- Warning    │
              │- Info       │
              │- Success    │
              └─────────────┘
```

---

## Integration Points

### 1. Topic Input (Real-time)
```swift
TextField("Topic", text: $topic)
    .withTopicIntelligence(topic: $topic) { suggestion in
        // User selected suggestion
    }
```

### 2. Batch Onboarding
```swift
let topics = ["Politics", "Tech", "Sports"]
let analyses = await analyzeTopics(topics)
// Show summary/warnings before confirming
```

### 3. Article Fetching (Automatic)
```javascript
// In fetchArticlesForTopic()
const analysis = await analyzeTopicSpecificity(topic)
const searchTerms = analysis.expandedTerms
// Use searchTerms for MongoDB query
```

### 4. Periodic Refinement
```swift
// Check after 20+ articles with feedback
if articleCount > 20 {
    let refinement = await learnFromFeedback(topic)
    if refinement.confidence > 70 {
        showRefinementSuggestion()
    }
}
```

---

## Error Handling & Fallbacks

```
┌──────────────────────┐
│  API Call            │
└──────────┬───────────┘
           │
    ┌──────▼──────┐
    │ OpenAI GPT  │
    └──────┬──────┘
           │
     ┌─────┴─────┐
     │           │
  Success    Failure
     │           │
     │     ┌─────▼─────────┐
     │     │ - No API key  │
     │     │ - Rate limit  │
     │     │ - Network err │
     │     └─────┬─────────┘
     │           │
     │     ┌─────▼─────────┐
     │     │  Fallback to  │
     │     │  Heuristics   │
     │     └─────┬─────────┘
     │           │
     └───────────┴───────────┐
                             │
                    ┌────────▼────────┐
                    │ Return Analysis │
                    │ (User never     │
                    │  sees error)    │
                    └─────────────────┘
```

**Graceful Degradation:**
- GPT unavailable → Use heuristics (80% accuracy)
- Network error → Cache last analysis
- API error → Allow user to proceed with warning
- **User experience never breaks** ✅

---

## Performance Optimization

### 1. Caching Strategy
```
┌──────────────────────┐
│  Common Topic?       │
│  (Politics, Sports)  │
└──────────┬───────────┘
           │
       ┌───┴───┐
       │       │
      Yes     No
       │       │
   ┌───▼───┐   │
   │ Cache │   │
   │ Lookup│   │
   └───┬───┘   │
       │       │
    Hit/Miss   │
       │       │
       └───┬───┘
           │
    ┌──────▼──────┐
    │ API Call    │
    │ (if needed) │
    └──────┬──────┘
           │
    ┌──────▼──────┐
    │ Cache Result│
    └─────────────┘
```

### 2. Debouncing
```
User types: P → Po → Pol → Poli → Polit → Politics
            │    │     │      │       │        │
            ├────┼─────┼──────┼───────┼────────┤
            └────┴─────┴──────┴───────┘        │
                                        ┌───────▼────────┐
                                        │ Wait 800ms     │
                                        └───────┬────────┘
                                                │
                                        ┌───────▼────────┐
                                        │ Analyze once   │
                                        └────────────────┘
```

### 3. Batch Processing
```
Instead of:              Use:
- Analyze "Politics"     - Analyze batch:
- Analyze "Sports"         ["Politics",
- Analyze "Tech"           "Sports",
                           "Tech"]
(3 API calls)            (1 API call)
~1500ms total            ~1000ms total
```

---

## Security & Privacy

### 1. Authentication
```
iOS App ──┐
          │ JWT Token
          ├──────────► Backend ──┐
          │                      │ Validate
          │                      └─► User ID
          │
          └──────────► Only accesses
                       own data
```

### 2. Data Flow
```
User Topic → Backend → OpenAI
     │          │         │
     │          │         └─► Anonymized
     │          │             (no user ID)
     │          │
     │          └─► Stored with
     │              user context
     │
     └─► Never shared
         with 3rd parties
```

---

## Scalability

### Current Implementation
- **Handles:** 1000+ users
- **Cost:** ~$5-10/month
- **Latency:** <1s per analysis

### Scale to 100K users
- Add Redis cache for common topics
- Use batch processing for onboarding
- Consider self-hosted models (cost optimization)
- Implement rate limiting per user

---

## Monitoring & Metrics

### Key Metrics to Track
1. **Analysis accuracy** (GPT vs heuristic)
2. **Suggestion acceptance rate**
3. **Average articles found** (before/after)
4. **User satisfaction** (feedback scores)
5. **API costs** (OpenAI usage)
6. **Response times** (p50, p95, p99)

### Logging
```javascript
console.log('[TOPIC_INTELLIGENCE] Analyzed "Politics": too_broad, 723ms');
console.log('[SMART_SEARCH] Expanded "RNA research" to 5 terms, found 15 articles');
console.log('[FEEDBACK_LEARNING] Suggested refinement: Politics → U.S. Elections (85% confidence)');
```

---

## Testing Architecture

```
┌──────────────────────────────────────┐
│    Unit Tests (Jest/Mocha)           │
│  - analyzeTopicSpecificity()         │
│  - generateSearchTerms()             │
│  - fallbackAnalysis()                │
└──────────────┬───────────────────────┘
               │
┌──────────────▼───────────────────────┐
│    Integration Tests                 │
│  - API endpoints                     │
│  - Error handling                    │
│  - Fallback behavior                 │
└──────────────┬───────────────────────┘
               │
┌──────────────▼───────────────────────┐
│    E2E Tests (iOS)                   │
│  - User adds topic                   │
│  - Suggestions displayed             │
│  - Articles fetched correctly        │
└──────────────────────────────────────┘
```

---

## Summary

This architecture provides:
- ✅ **Modular design** - Easy to modify/extend
- ✅ **Graceful degradation** - Works without GPT
- ✅ **Real-time feedback** - Immediate suggestions
- ✅ **Smart search** - Better article matching
- ✅ **Learning system** - Improves over time
- ✅ **Cost-effective** - ~$5/month
- ✅ **Scalable** - Handles growth easily

The system seamlessly integrates into your existing infrastructure while providing significant UX improvements. 🚀
