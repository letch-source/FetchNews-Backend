# Required Environment Variables for Production

# JWT Secret (REQUIRED) - Generate a strong secret for production
JWT_SECRET=your-super-secure-jwt-secret-here

# News API Key (REQUIRED) - Get from https://newsapi.org/
NEWSAPI_KEY=your-newsapi-key-here

# OpenAI API Key (REQUIRED for TTS functionality) - Get from https://platform.openai.com/
OPENAI_API_KEY=your-openai-api-key-here

# Frontend URL (REQUIRED for CORS) - Your deployed frontend URL
FRONTEND_ORIGIN=https://your-frontend-app.onrender.com

# Node Environment
NODE_ENV=production

# Port (Render will set this automatically)
PORT=10000

# Redis URL for caching (optional - will use in-memory fallback if not provided)
REDIS_URL=redis://localhost:6379

# MongoDB connection string (REQUIRED for user accounts)
MONGODB_URI=mongodb://localhost:27017/fetchnews

# ElevenLabs API key for better TTS (optional)
ELEVENLABS_API_KEY=your-elevenlabs-key-here
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# Backblaze B2 Cloud Storage (REQUIRED for audio file persistence)
B2_KEY_ID=your-b2-key-id-here
B2_APPLICATION_KEY=your-b2-application-key-here
B2_BUCKET_NAME=your-bucket-name-here
B2_ENDPOINT=s3.us-west-004.backblazeb2.com
B2_REGION=us-west-004

# Google OAuth (REQUIRED for authentication)
# Get this from Google Cloud Console -> Credentials -> OAuth 2.0 Client ID (Web application)
GOOGLE_CLIENT_ID=your-google-web-client-id.apps.googleusercontent.com

# Email Configuration (REQUIRED for password reset and email verification)
# SMTP settings for sending emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
SMTP_FROM_EMAIL=noreply@fetchnews.app

# For Gmail: Use an App Password (https://myaccount.google.com/apppasswords)
# For SendGrid: SMTP_USER=apikey, SMTP_PASSWORD=your-sendgrid-api-key
# SMTP_FROM_EMAIL must be verified in SendGrid (Settings -> Sender Authentication)

# APNs Configuration (REQUIRED for push notifications)
# Get these from Apple Developer Portal -> Certificates, Identifiers & Profiles -> Keys
# Create a new key with "Apple Push Notifications service (APNs)" enabled
APN_KEY_ID=your-apns-key-id-here
APN_TEAM_ID=your-apple-team-id-here
APN_BUNDLE_ID=com.fetchnews.app
# Paste the entire contents of your .p8 key file here (including BEGIN/END lines)
APN_KEY_CONTENT=-----BEGIN PRIVATE KEY-----
...your key content here...
-----END PRIVATE KEY-----
# Alternative: Use file path instead of content
# APN_KEY_PATH=/path/to/AuthKey_ABC123DEFG.p8